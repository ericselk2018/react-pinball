import Light from '@/engine/entities/Light';
import { rightKickerButton } from '../buttons/buttons';

const rightKickerLight: Light = {
	id: 0,
	button: rightKickerButton,
};

const lights: Light[] = [rightKickerLight];

export default lights;
